<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="fr.isen.gunia.androiderestaurant">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidERestaurant"
        android:usesCleartextTraffic="true"
        tools:ignore="ExtraText">
        <activity android:name=".network.Price"></activity>
        <activity android:name=".network.NetworkConstant" />
        <activity android:name=".network.MenuResult" />
        <activity android:name=".network.Ingredient" />
        <activity android:name=".network.Dish" />
        <activity android:name=".network.Category" />
        <activity android:name=".details.DetailsActivity" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>